{% include 'base.html.twig' %}

    {% block body %}

  <h3 class="d-flex justify-content-center mt-5">{{message}}</h3>
  <div class="d-flex justify-content-center">
    <a href="{{path('homepage')}}"><button type="button" class="btn btn-success">Générer une excuse</button></a>
  </div>

{% endblock %} 